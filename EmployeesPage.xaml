<Page x:Class="AssetManagment.Pages.EmployeesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="900" d:DesignWidth="1600"
      Title="EmployeesPage"
      Background="#F8FAFC">

    <Page.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" Direction="-90" RenderingBias="Quality" ShadowDepth="4" Opacity="0.1" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActionButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="8,0"/>
        </Style>
        <Style x:Key="PopupButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,0"/>
        </Style>
    </Page.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ============================================ -->
        <!-- СТАТИСТИКА -->
        <!-- ============================================ -->
        <UniformGrid Grid.Row="0" Rows="1" Columns="4" Margin="0,0,0,24">
            <!-- Всего сотрудников -->
            <Border Style="{StaticResource CardStyle}" Padding="24" Margin="0,0,12,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Всего сотрудников" FontSize="13" Foreground="#64748B" FontWeight="Medium"/>
                        <TextBlock x:Name="txtTotalEmployees" Text="0" FontSize="32" FontWeight="Bold" Foreground="#1F2937" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#EEF2FF" CornerRadius="12" Width="48" Height="48" VerticalAlignment="Top">
                        <materialDesign:PackIcon Kind="AccountGroup" Width="24" Height="24" Foreground="#6366F1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Активных -->
            <Border Style="{StaticResource CardStyle}" Padding="24" Margin="6,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Активных" FontSize="13" Foreground="#64748B" FontWeight="Medium"/>
                        <TextBlock x:Name="txtActiveEmployees" Text="0" FontSize="32" FontWeight="Bold" Foreground="#10B981" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#ECFDF5" CornerRadius="12" Width="48" Height="48" VerticalAlignment="Top">
                        <materialDesign:PackIcon Kind="AccountCheck" Width="24" Height="24" Foreground="#10B981" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Отделов -->
            <Border Style="{StaticResource CardStyle}" Padding="24" Margin="6,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Отделов" FontSize="13" Foreground="#64748B" FontWeight="Medium"/>
                        <TextBlock x:Name="txtDepartmentCount" Text="0" FontSize="32" FontWeight="Bold" Foreground="#8B5CF6" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#F3E8FF" CornerRadius="12" Width="48" Height="48" VerticalAlignment="Top">
                        <materialDesign:PackIcon Kind="Domain" Width="24" Height="24" Foreground="#8B5CF6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Новых за месяц -->
            <Border Style="{StaticResource CardStyle}" Padding="24" Margin="12,0,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Новых за месяц" FontSize="13" Foreground="#64748B" FontWeight="Medium"/>
                        <TextBlock x:Name="txtNewEmployees" Text="0" FontSize="32" FontWeight="Bold" Foreground="#EC4899" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#FCE7F3" CornerRadius="12" Width="48" Height="48" VerticalAlignment="Top">
                        <materialDesign:PackIcon Kind="AccountPlus" Width="24" Height="24" Foreground="#EC4899" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </UniformGrid>

        <!-- ============================================ -->
        <!-- ШАПКА -->
        <!-- ============================================ -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="32" Margin="0,0,0,24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#EEF2FF" CornerRadius="12" Width="48" Height="48" Margin="0,0,16,0">
                            <materialDesign:PackIcon Kind="AccountGroup" Width="24" Height="24" Foreground="#6366F1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Управление сотрудниками" FontSize="24" FontWeight="Bold" Foreground="#1F2937"/>
                            <TextBlock Text="Просмотр и редактирование информации о персонале" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button x:Name="btnAddEmployee" Style="{StaticResource ActionButtonStyle}" Background="#6366F1" Foreground="White" Click="BtnAddEmployee_Click">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="AccountPlus" VerticalAlignment="Center" Width="20" Height="20" Margin="0,0,8,0"/>
                                <TextBlock Text="Добавить сотрудника"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="1" Margin="0,32,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Height="48">
                        <Grid>
                            <materialDesign:PackIcon Kind="Magnify" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="16,0,0,0" Width="20" Height="20" Foreground="#94A3B8" IsHitTestVisible="False"/>
                            <TextBox x:Name="txtSearch" Padding="48,0,40,0" BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center" FontSize="14" TextChanged="TxtSearch_TextChanged"/>
                            <TextBlock Text="Поиск по ФИО, email, телефону..." HorizontalAlignment="Left" VerticalAlignment="Center" Margin="48,0,0,0" Foreground="#94A3B8" IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=txtSearch, Path=Text}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="24,0,0,0" VerticalAlignment="Center">
                        <Border Background="#F8FAFC" CornerRadius="8" Padding="12,8" Margin="0,0,12,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Filter" Width="16" Height="16" VerticalAlignment="Center" Foreground="#64748B" Margin="0,0,8,0"/>
                                <TextBlock Text="Фильтры:" FontSize="13" FontWeight="Medium" Foreground="#475569" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ComboBox x:Name="cmbDepartmentFilter" materialDesign:HintAssist.Hint="Отдел" Style="{StaticResource MaterialDesignOutlinedComboBox}" Width="200" Height="48" Margin="0,0,12,0" SelectionChanged="CmbFilter_SelectionChanged" IsEditable="False"/>
                        <ComboBox x:Name="cmbPositionFilter" materialDesign:HintAssist.Hint="Должность" Style="{StaticResource MaterialDesignOutlinedComboBox}" Width="200" Height="48" Margin="0,0,12,0" SelectionChanged="CmbFilter_SelectionChanged" IsEditable="False"/>
                        <ComboBox x:Name="cmbStatusFilter" materialDesign:HintAssist.Hint="Статус" Style="{StaticResource MaterialDesignOutlinedComboBox}" Width="150" Height="48" SelectionChanged="CmbFilter_SelectionChanged" IsEditable="False"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- ============================================ -->
        <!-- ТАБЛИЦА -->
        <!-- ============================================ -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <DataGrid x:Name="dgEmployees" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="None" SelectionMode="Single" Background="Transparent" 
                      materialDesign:DataGridAssist.CellPadding="16 12" materialDesign:DataGridAssist.ColumnHeaderPadding="16 12"
                      MouseDoubleClick="DgEmployees_MouseDoubleClick">
                <DataGrid.Resources>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#EEF2FF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#475569"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Height" Value="48"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <!-- СОТРУДНИК -->
                    <DataGridTemplateColumn Header="СОТРУДНИК" Width="*" MinWidth="320">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <!-- ИСПРАВЛЕННЫЙ БЛОК ФОТО -->
                                    <Grid Width="48" Height="48" Margin="0,0,16,0">
                                        <!-- Фото если есть -->
                                        <Ellipse>
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ProfileImage}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                            <Ellipse.Fill>
                                                <ImageBrush ImageSource="{Binding ProfileImage}" Stretch="UniformToFill"/>
                                            </Ellipse.Fill>
                                        </Ellipse>

                                        <!-- Инициалы если нет фото -->
                                        <Border CornerRadius="24" Background="#EEF2FF">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ProfileImage}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding Initials}" FontSize="16" FontWeight="Bold" 
                                                       Foreground="#6366F1" HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                    </Grid>

                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding FullName}" FontSize="14" FontWeight="SemiBold" Foreground="#1F2937"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <materialDesign:PackIcon Kind="Email" Width="14" Height="14" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <TextBlock Text="{Binding Email}" FontSize="12" Foreground="#64748B"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- ДОЛЖНОСТЬ -->
                    <DataGridTemplateColumn Header="ДОЛЖНОСТЬ" Width="250">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding PositionName}" FontSize="14" FontWeight="Medium" Foreground="#1F2937"/>
                                    <TextBlock Text="{Binding DepartmentName}" FontSize="12" Foreground="#64748B" Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- ТЕЛЕФОН -->
                    <DataGridTextColumn Header="ТЕЛЕФОН" Binding="{Binding Phone}" Width="200">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#475569"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- ДАТА НАЙМА -->
                    <DataGridTextColumn Header="ДАТА НАЙМА" Binding="{Binding HireDate, StringFormat=dd.MM.yyyy}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#475569"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- СТАТУС -->
                    <DataGridTemplateColumn Header="СТАТУС" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="12,6">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#ECFDF5"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Background" Value="#FEF2F2"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding StatusText}" FontSize="12" FontWeight="Medium" HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#10B981"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Foreground" Value="#EF4444"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- ДЕЙСТВИЯ -->
                    <DataGridTemplateColumn Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <materialDesign:PopupBox PlacementMode="BottomAndAlignRightEdges" StaysOpen="False" Width="32" Height="32">
                                        <StackPanel Width="200">
                                            <Button x:Name="BtnEdit" Style="{StaticResource PopupButtonStyle}" Click="BtnEdit_Click" CommandParameter="{Binding}">
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="Pencil" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="#475569"/>
                                                    <TextBlock Text="Редактировать"/>
                                                </StackPanel>
                                            </Button>
                                            <Button x:Name="BtnToggleStatus" Style="{StaticResource PopupButtonStyle}" Click="BtnToggleStatus_Click" CommandParameter="{Binding}">
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="AccountSwitch" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="#F59E0B"/>
                                                    <TextBlock Text="Изменить статус"/>
                                                </StackPanel>
                                            </Button>
                                            <Separator Margin="0,8"/>
                                            <Button x:Name="BtnDeleteEmployee" Style="{StaticResource PopupButtonStyle}" Foreground="#EF4444" Click="BtnDelete_Click" CommandParameter="{Binding}">
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="DeleteForever" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                                    <TextBlock Text="Удалить навсегда"/>
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </materialDesign:PopupBox>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>