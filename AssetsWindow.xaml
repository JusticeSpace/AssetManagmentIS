<Window x:Class="AssetManagment.AssetsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление активами" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" Background="White" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="0,0,0,1">
            <Grid Margin="30,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Заголовок -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📦" FontSize="28" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Управление активами" FontSize="24" FontWeight="SemiBold" 
                             Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>

                <!-- Панель действий -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Поиск -->
                    <Border Background="#F8F9FA" CornerRadius="20" Margin="0,0,15,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="250"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔍" Grid.Column="0" 
                                     VerticalAlignment="Center" 
                                     Margin="15,0,5,0" 
                                     Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBox x:Name="txtSearch" Grid.Column="1" 
                                   Background="Transparent"
                                   BorderThickness="0"
                                   VerticalAlignment="Center"
                                   Padding="5,10,15,10"
                                   FontSize="14"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   TextChanged="TxtSearch_TextChanged">
                                <TextBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="20"/>
                                    </Style>
                                </TextBox.Resources>
                            </TextBox>
                        </Grid>
                    </Border>

                    <!-- Кнопки действий -->
                    <Button Style="{StaticResource PrimaryButton}" Click="BtnAdd_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="➕" Margin="0,0,8,0"/>
                            <TextBlock Text="Добавить"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource SecondaryButton}" Click="BtnEdit_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✏️" Margin="0,0,8,0"/>
                            <TextBlock Text="Изменить"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource DangerButton}" Click="BtnDelete_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🗑️" Margin="0,0,8,0"/>
                            <TextBlock Text="Удалить"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <Grid Grid.Row="1" Margin="20">
            <Border Style="{StaticResource DashboardCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок таблицы с счетчиком -->
                    <Border Grid.Row="0" Background="#FAFBFC" 
                            CornerRadius="10,10,0,0" 
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="0,0,0,1">
                        <Grid Margin="20,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="Список активов" FontSize="16" FontWeight="SemiBold" 
                                         Foreground="{StaticResource TextPrimaryBrush}"/>
                                <Border Background="{StaticResource PrimaryBrush}" 
                                        CornerRadius="10" 
                                        Padding="10,3" 
                                        Margin="15,0,0,0">
                                    <TextBlock Foreground="White" FontSize="12" FontWeight="Medium">
                                        <Run Text="{Binding ElementName=dgAssets, Path=Items.Count}"/>
                                        <Run Text="записей"/>
                                    </TextBlock>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="🔄 Обновить" 
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="{StaticResource TextSecondaryBrush}"
                                        Cursor="Hand"
                                        Click="BtnRefresh_Click"/>
                                <Button Content="📥 Экспорт" 
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="{StaticResource TextSecondaryBrush}"
                                        Cursor="Hand"
                                        Margin="10,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Таблица данных -->
                    <DataGrid x:Name="dgAssets" 
                              Grid.Row="1"
                              Style="{StaticResource ModernDataGrid}"
                              ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                              RowStyle="{StaticResource ModernDataGridRow}"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              Margin="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Код" 
                                              Binding="{Binding AssetCode}" 
                                              Width="100"/>
                            <DataGridTextColumn Header="Наименование" 
                                              Binding="{Binding AssetName}" 
                                              Width="*"
                                              MinWidth="200"/>
                            <DataGridTextColumn Header="Категория" 
                                              Binding="{Binding CategoryName}" 
                                              Width="150"/>
                            <DataGridTemplateColumn Header="Статус" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding StatusColor}" 
                                                CornerRadius="10" 
                                                Padding="10,3"
                                                HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding StatusName}" 
                                                     Foreground="White" 
                                                     FontSize="12"
                                                     FontWeight="Medium"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Локация" 
                                              Binding="{Binding LocationName}" 
                                              Width="150"/>
                            <DataGridTextColumn Header="Ответственный" 
                                              Binding="{Binding ResponsiblePerson}" 
                                              Width="200"/>
                            <DataGridTextColumn Header="Стоимость" 
                                              Binding="{Binding PurchasePrice, StringFormat='{}{0:C}'}" 
                                              Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextAlignment" Value="Right"/>
                                        <Setter Property="Margin" Value="0,0,10,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- Нижняя панель статуса -->
        <Border Grid.Row="2" Background="White" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="0,1,0,0">
            <Grid Margin="30,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                         VerticalAlignment="Center"
                         Foreground="{StaticResource TextSecondaryBrush}">
                    <Run Text="Выбрано:"/>
                    <Run Text="{Binding ElementName=dgAssets, Path=SelectedItems.Count}" FontWeight="SemiBold"/>
                    <Run Text="из"/>
                    <Run Text="{Binding ElementName=dgAssets, Path=Items.Count}" FontWeight="SemiBold"/>
                </TextBlock>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Последнее обновление:" 
                             Foreground="{StaticResource TextSecondaryBrush}"
                             VerticalAlignment="Center"
                             Margin="0,0,5,0"/>
                    <TextBlock x:Name="txtLastUpdate" 
                             Text="только что" 
                             Foreground="{StaticResource TextPrimaryBrush}"
                             FontWeight="Medium"
                             VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>